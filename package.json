{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/sage-*"
  ],
  "devDependencies": {
    "eslint": "^7.10.0",
    "lerna": "^3.22.1",
    "npm-run-all": "4.1.5"
  },
  "scripts": {
    "bootstrap": "yarn lerna bootstrap",
    "build": "yarn npm-run-all --parallel build:*",
    "build:assets": "cd packages/sage-assets && yarn build:watch",
    "build:react": "cd packages/sage-react && yarn build:watch",
    "build:system": "cd packages/sage-system && yarn build:watch"
  }
}
