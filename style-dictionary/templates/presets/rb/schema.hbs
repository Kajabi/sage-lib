#
# Sage Component Schemas
#
# Note: The contents of this file are generated by Style Dictionary
# and should not be modified directly here.
# See `/style-dictionary` directory for tokens and configurations.
#

module SageConfigs

  TBD = ''

{{#each components}}

  {{#if name}}

  #
  # {{name}}
  #
  module {{name}}
  {{!-- First look in props for any with options and output options tokens --}}
    TOKENS = {
    {{#each props}}
      {{#if options}}
      {{name}}: {
        {{#each options}}
        {{name}}: {{{asRubyPrimitive value}}},
        {{/each}}
      },
      {{/if}}
    {{/each}}
    }

    {{!-- Next look in props for any defaults to set up --}}
    DEFAULTS = {
    {{#each props}}
      {{#if (isPresent default)}}
      {{name}}: {{{asRubyPrimitive default}}},
      {{/if}}
    {{/each}}
    }

    {{!-- Lastly, output the actual Props type declaration --}}
    SCHEMA = {
    {{#each props}}
      {{#if comment}}
      # {{{comment}}}
      {{/if}}
      {{name}}: [
        {{#if required}}{{else}}:optional,{{/if}}
      {{#if options}}
        Set.new(TOKENS[:{{name}}].values),
      {{else}}
        {{{schema.rb.value}}},
      {{/if}}
      ],
    {{/each}}
    }

    {{!-- Output the actual Props type declaration --}}
    SECTIONS = %w({{#each sections}}{{lowercase ../name}}_{{.}} {{/each}})
  end
  {{/if}}
{{/each}}
end
