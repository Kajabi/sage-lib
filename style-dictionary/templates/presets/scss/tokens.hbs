////
/// Sage Dictionary tokens
///
/// Note: The contents of this file are generated by Style Dictionary
/// and should not be modified directly here.
/// See `/style-dictionary` directory for tokens and configurations.
////

{{#each categories}}
/// ----------
///
/// Category: {{category}}
///
/// ----------

  {{#each types}}
///
/// Type: {{type}}
///

///
/// Static vars for {{../category}} {{type}}
///
    {{#each tokens}}
$sage-{{../../category}}-{{../type}}-{{name}}: {{#if value.value}}{{{value.value}}}{{else}}{{{value}}}{{/if}};
    {{/each}}

///
/// Map for {{../category}} {{type}}
///
$sage-{{../category}}{{#if (notBaseType type)}}-{{type}}{{/if}}: (
    {{#each items}}
      {{#if (hasAny subitems)}}
  {{item}}: (
        {{#each subitems}}
    {{subitem}}: $sage-{{../../../category}}-{{../../type}}-{{../item}}-{{subitem}},
        {{/each}}
  ),
      {{else}}
  {{item}}: $sage-{{../../category}}-{{../type}}-{{item}},
      {{/if}}
    {{/each}}
);

///
/// Sage {{../category}} {{type}} token utility
///
/// @param {string} $key [default] The token to retrieve
    {{#if (hasAnySubitems items)}}
/// @param {string} $subkey [defult] The subtoken to retrieve
    {{/if}}
///
@function sage-{{../category}}{{#if (notBaseType type)}}-{{type}}{{/if}}($key: default{{#if (hasAnySubitems items)}}, $subkey: default{{/if}}) {
  $value: map-get($sage-{{../category}}-{{type}}, $key);
    {{#if (hasAnySubitems items)}}
  $value: map-get($value, $subkey);
    {{/if}}
  @return $value;
}

  {{/each}}
{{/each}}
