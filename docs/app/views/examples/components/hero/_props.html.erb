<tr>
  <td><%= md('`cta_attributes`') %></td>
  <td><%= md('Allows for adding attributes to the Hero CTA artwork') %></td>
  <td><%= md('Array') %></td>
  <td><%= md('') %></td>
</tr>
<tr>
  <td><%= md('`description`') %></td>
  <td><%= md('Sets description text for the Hero') %></td>
  <td><%= md('String') %></td>
  <td><%= md('`nil`') %></td>
</tr>
<tr>
  <td><%= md('`image`') %></td>
  <td><%= md('Image for the Hero.') %></td>
  <td>
      <%= md('```
Hash<{
  src: String,
  alt: String
}>
    ') %>
  <td><%= md('`nil`') %></td>
</tr>
<tr>
  <td><%= md('`hero_size`') %></td>
  <td><%= md('Size for the hero container.') %></td>
  <td><%= md('String: ["small", "large"]') %></td>
  <td><%= md('`large`') %></td>
</tr>
<tr>
  <td><%= md('`title`') %></td>
  <td><%= md('Sets the title for the component.') %></td>
  <td><%= md('String') %></td>
  <td><%= md('`nil`') %></td>
</tr>
<tr>
  <td><%= md('`title_tag`') %></td>
  <td><%= md('Sets the heading tag (`h1` - `h6`) for the Hero\'s title') %></td>
  <td><%= md('String') %></td>
  <td><%= md('`h2`') %></td>
</tr>
<tr>
  <td colspan="2"><%= md('**Sections**') %></td>
  <td colspan="2"><%= md('**`Element`**') %></td>
</tr>
<tr>
  <td><%= md('`sage_hero_branding`') %></td>
  <td><%= md('This area holds the branding elements of the hero component.') %></td>
  <td colspan="2"><%= md('`SageCardRow`') %></td>
</tr>
<tr>
  <td><%= md('`sage_hero_footer_actions`') %></td>
  <td><%= md('This area holds the action bottons at the bottom of the component.') %></td>
  <td colspan="2"><%= md('`SageButton`s') %></td>
</tr>

