<%= md("
- Use inline `<b>` tag to add bolding to select portions of these blocks.
- Provide a local max-width as desired in order to limit overflowing text.
  Truncation is setup and ready for this by default on the inline Copy Text,
  but can be turned on for the Copy Text Card with `truncate_contents` set to `true`.
", use_sage_type: true) %>
