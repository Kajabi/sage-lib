<%= md("
- Don't forget that some browsers read elements differently when enumeration or bullets are removed with `list-style: none`.
  `aria-role=\"listitem\"` is useful in such cases.
", use_sage_type: true) %>
