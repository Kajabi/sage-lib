<tr>
  <td><%= md('`items`') %><%= sage_component SageLabel, { color: "published", value: "required" } %></td>
  <td><%= md('Items to show in the legend') %></td>
  <td><%= md('```
Array<{
  title: String,
  color: String (Hex value),
}>
```') %></td>
  <td><%= md('`nil`') %></td>
</tr>
