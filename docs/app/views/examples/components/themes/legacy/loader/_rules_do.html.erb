<%= md("
- Toggle the loader's active state using the `\"data-loading\"` attribute
- Take note that by design, a \"reduced motion\" [OS-level setting](https://web.dev/prefers-reduced-motion/)
  will prevent either loader from appearing
", use_sage_type: true) %>
