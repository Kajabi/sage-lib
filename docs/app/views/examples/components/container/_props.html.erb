<tr>
  <td><%= md('`size`') %></td>
  <td><%= md("Sets the maximum size for the container. See the <a href=\"#{pages_layout_path(:container)}\">Layout Containers</a> page.") %></td>
  <td><%= md("`\"#{SageTokens::CONTAINER_SIZES.join("\"`, `\"")}\"`") %></td>
  <td><%= md('`"full"`') %></td>
</tr>
