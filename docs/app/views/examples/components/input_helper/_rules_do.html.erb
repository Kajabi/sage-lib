<%= md("
- The input helper expects a surrounding `sage-input-group`, which helps position the window's appearance
- Be sure to match the `helper_target` with the intended input's ID to make use of the password strength functions
", use_sage_type: true) %>
