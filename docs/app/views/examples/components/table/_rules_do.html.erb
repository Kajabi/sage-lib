<%= md("
- Make use of the `thead`, `tbody`, and `tfoot` elements.
  While not required, they provide a useful indicator of semantics for accessibility purposes.
- Use the `<caption>` element to
  [provide additional description to the table](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/caption)
", use_sage_type: true) %>
