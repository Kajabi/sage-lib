<tr>
  <td><%= md('`center_vertical`') %></td>
  <td><%= md('If true, the empty state will be adjust to be visually centered inside the entire page context. Meant to be used in pairing with `scope: "page"`.') %></td>
  <td><%= md('Boolean') %></td>
  <td><%= md('`false`') %></td>
</tr>
<tr>
  <td><%= md('DEPRECATED: `compact`') %></td>
  <td><%= md('Deprecated. Use `scope: "compact"` instead.') %></td>
  <td><%= md('Boolean') %></td>
  <td><%= md('`false`') %></td>
</tr>
<tr>
  <td><%= md('`icon`') %></td>
  <td><%= md('Adds an icon above the content.') %></td>
  <td><%= md('String') %></td>
  <td><%= md('`nil | Icon name from Sage icons`') %></td>
</tr>
<tr>
  <td><%= md('`graphic`') %></td>
  <td><%= md('Adds a graphic above the content.') %></td>
  <td><%= md('String') %></td>
  <td><%= md('`nil`') %></td>
</tr>
<tr>
  <td><%= md('`scope`') %></td>
  <td><%= md('The layout of the component adjusts depending on this context setting:
  
- `nil` (default) sets up a "feature"-level layout for use within panels or cards for showing empty state for particular features.
- `"compact"` similar to the default above, this compacts the spacing a little more for smaller contexts.
- `"page"` is for use on whole page empty states and is intended to fill the stage.
') %></td>
  <td><%= md('`nil` | `"compact"` | `"page"`') %></td>
  <td><%= md('`nil`') %></td>
</tr>
<tr>
  <td><%= md('`text`') %></td>
  <td><%= md('Text for empty state.') %></td>
  <td><%= md('String') %></td>
  <td><%= md('`nil`') %></td>
</tr>
<tr>
  <td><%= md('`title`') %></td>
  <td><%= md('Title for empty state.') %></td>
  <td><%= md('String') %></td>
  <td><%= md('`nil`') %></td>
</tr>
<tr>
  <td><%= md('`title_tag`') %></td>
  <td><%= md('Sets which HTML heading tag to use on the title.') %></td>
  <td><%= md('One of `h1` through `h6`') %></td>
  <td><%= md('`h2`') %></td>
</tr>
