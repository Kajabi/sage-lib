<%= md("
- Use the `sage-btn-group` for more than one button to apply spacing
- If the context does not already provide positioning settings (such as inside a card or panel footer) 
  use the `sage-btn-group--align-end` to place buttons on the right side of a space.
", use_sage_type: true) %>
