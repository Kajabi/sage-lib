<%# Desired HTML %>
<div class="
    sage-video-thumbnail
    <%= component.generated_css_classes %>
  "
  <%= component.generated_html_attributes.html_safe %>
>
  <a
    <% component.cta_attributes.each do |key, value| %>
        <%= "#{key}='#{value}'".html_safe %>
      <% end if component.cta_attributes&.is_a?(Hash) %>
    class="sage-video-thumbnail__play-button"
  >
    <img class="sage-video-thumbnail__image" src="<%= component.image_url %>" alt="" />
    <span>Play media</span>
  </a>
</div>
