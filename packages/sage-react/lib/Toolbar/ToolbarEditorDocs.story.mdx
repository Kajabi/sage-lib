import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';

import * as Stories from './ToolbarEditor.story.jsx';
import * as ButtonStories from '../Button/Button.story.jsx';
import * as DropdownStories from '../Dropdown/Dropdown.story.jsx';
import { ToolbarEditor } from './ToolbarEditor';

<Meta
  title="Sage/ToolbarEditor"
  component={ToolbarEditor}
/>

# Toolbar Editor MDX
<Canvas>
  <Story story={Stories.Default} />
</Canvas>

Toolbar Editor is used for places like toolbar.

You'll need to wrap your `<ToolbarEditor />`s inside of a `<Toolbar gap={Toolbar.Gap.XS} />` to receive the desired visual treatment.


## Button types
#### Rich Text Editor Button

#### Rich Text Editor Dropdown Button
<Canvas>
  <Story story={ButtonStories.RichTextEditorWordsAndArrow} />
</Canvas>

#### Rich Text Editor Dropdown Button with Icon
<Canvas>
  <Story story={ButtonStories.RichTextEditorIconAndArrow} />
</Canvas>

### Selected buttons
Add the `selected` attribute to your `<SageButton />`s to receive the selected treatment.

# Properties
The available properties for `ToolbarEditor`.

<ArgsTable of={ToolbarEditor} />
