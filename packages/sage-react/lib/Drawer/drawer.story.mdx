import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Sage/Drawer/Notes" />

The Drawer allows us to format a drawer window according to Sage design specs and includes the following features:

- `Drawer.Header` -- Formats the header of the drawer.
- `Drawer.Body` -- Minimal requirement to properly format the contents of the Drawer.